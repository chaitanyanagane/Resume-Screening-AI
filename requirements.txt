# Runtime matching and analytics
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# The API uses TF-IDF matching and rule-based explanations. SHAP, Numba,
# llvmlite, XGBoost, Sentence-Transformers, PyTorch, and CUDA packages are not
# required at runtime and are intentionally excluded from this deployment set.

# Resume Parsing
PyPDF2>=3.0.0
python-docx>=1.1.0

# API & Auth
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pyjwt>=2.8.0
bcrypt>=4.1.0
python-multipart>=0.0.9
email-validator>=2.0.0

# Database & Config
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic-settings>=2.0.0
